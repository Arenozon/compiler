hello = 1;
func hello(i64 x, i64 y) {
	if (x == y) {
		x = y;
	} elif (x >= y) {
		x = x - 1;
	} elif (x <= y + $hotdog()) {
		x = x + 1 - $apple();
	} else {
		x = 0;
	}
}

while (something != something) {
	i64 somethingelse;

	$apple(x, y);
}
